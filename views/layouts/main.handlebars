<!DOCTYPE html>
<html>
    <head>
        <title>Sistema de Controle de Eventos</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css">
        <link rel="stylesheet" href="../css/style.css">
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js"></script>
    </head>
    <body>
        <span class="border-bottom-0"></span>
        <div class="d-flex justify-content-center bg-light border-bottom">
            <h1 class="text-dark">Sistema de Controle de Eventos culturais</h1>
        </div>
        <nav class="navbar navbar-expand-lg  navbar-light bg-info">
            <a class="navbar-brand" href="/">Pagina inicial</a>
            <button class="navbar-toggler" 
                    type="button" 
                    data-toggle="collapse" 
                    data-target="#navbarSupportedContent"
                    aria-controls="navbarSupportedContent" 
                    aria-expanded="false"
                    aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle"
                           href="#"
                           id="navbarDropdown"
                           role="button"
                           data-toggle="dropdown"
                           aria-haspopup="true"
                           aria-expanded="false">
                            Concursos
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            {{#if usuario.eAdmin}}
                                <a class="dropdown-item" href="/concurso/create">Cadastrar novo concurso</a>
                            {{/if}}
                            <a class="dropdown-item" href="/concurso/list">Listar todos os concursos</a>
                        </div>
                    </li>
                    {{#if usuario.eAdmin}}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle"
                               href="#"
                               id="navbarDropdown2"
                               role="button"
                               data-toggle="dropdown"
                               aria-haspopup="true"
                               aria-expanded="false">
                                Usuarios
                            </a>
                            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                                <a class="dropdown-item" href="/usuario/create">Cadastrar</a>
                                <a class="dropdown-item" href="/usuario/list">Listar</a>
                            </div>
                        </li>
                    {{/if}}
                    {{#if usuario.id}}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle"
                               href="#"
                               id="navbarDropdown2"
                               role="button"
                               data-toggle="dropdown"
                               aria-haspopup="true"
                               aria-expanded="false">
                                Meu perfil
                            </a>
                            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                                <a class="dropdown-item" href="/usuario/edit">Editar cadastro</a>
                                <a class="dropdown-item" href="/item/usuario/list">Ver itens enviados</a>
                            </div>
                        </li>
                    {{/if}}
                </ul>
                <ul class="navbar-nav ml-auto">
                    {{#if usuario.id}}
                        <li class="nav-item">
                            <a class="nav-link" href="/usuario/logout">Logout</a>
                        </li>
                    {{else}}
                        <li class="nav-item">
                            <a class="nav-link" href="/usuario/create">Criar conta</a>
                        </li>

                        <li class="nav-item">
                            <a class="nav-link" href="/usuario/login">Acessar conta</a>
                        </li>
                    {{/if}}
                </ul>
                </form>
            </div>
        </nav>
        <center>
            {{{body}}}
        </center>
    </body>

</html>
