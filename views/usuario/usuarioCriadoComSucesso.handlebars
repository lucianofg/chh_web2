
{{#if usuario.nome}}
    <h1>{{usuario.nome}} criado com sucesso!</h1>
{{else}}
    <h1>Erro na criação de usuário</h1>
    <p>{{error}}</p>
    <a href='/'><button class="btn btn-primary btn-lg">Voltar a página inicial</button></a>
{{/if}}
